# after-darts-weekly-ranking
Weekly COUNT-UP ranking for AFTER BAR MAKATI

The weekly progress workflow also creates a fixed-template Facebook image at
`generated/facebook-ranking-YYYY-MM-DD.png`. Only the top-three player names
and scores are drawn over `assets/social/facebook-ranking-base-4x5.png`.
